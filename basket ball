#basket ball
'''l=[]
g=int(input())
size=int(input())
a=list(map(int,input().split()))
mx=-1
for i in range(0,len(a)-size+1):
    temp=a[i:i+size]
    k,s=1,0
    for j in temp:
        s+=(j*k)
        k+=1
    if s>mx:
        mx=s
print(mx)'''
